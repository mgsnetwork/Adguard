! AdGuard Home — Adblock-style conservative allowlist for Envertech inverter
! One allow rule per line. Use @@||domain^ to allow domains and their subdomains.
! Start with this conservative set, then monitor AdGuard Home Query Log and add any exact hostnames your device needs.

@@||envertech.com^
@@||envertech.net^
@@||portal.envertech.com^
@@||api.envertech.com^
@@||cloud.envertech.com^
@@||telemetry.envertech.com^
@@||update.envertech.com^
@@||sync.envertech.com^
@@||firmware.envertech.com^

@@||pool.ntp.org^
@@||time.google.com^
@@||time.cloudflare.com^

@@||acme-v02.api.letsencrypt.org^
@@||letsencrypt.org^

@@||s3.amazonaws.com^
@@||cloudfront.net^

! Notes:
! - Prefer adding exact hostnames observed in Query Log instead of broad allow rules.
! - ||domain^ matches domain and subdomains; @@ makes it an allow rule for Adblock-style filters.
! - After applying, watch Query Log and add any required domains that are blocked.
